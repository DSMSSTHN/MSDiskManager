<Page x:Class="MSDiskManager.Pages.AddItems.AddItemsPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:MSDiskManager.Pages.AddItems"
        Loaded="AddPage_Loaded"
      mc:Ignorable="d" 
      ShowsNavigationUI="False"
      d:DesignHeight="1080" d:DesignWidth="1080"
      AllowDrop="True"
      Drop="Page_Drop"
      KeepAlive="False"
      Background="Transparent"
      Name="AddPage"
      DragDrop.PreviewDragEnter="HandleDragEnter"
      DragDrop.PreviewDragLeave="HandleDragLeave"
      Title="AddItemsPage">
    <Page.Resources>
        <ResourceDictionary >
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="../PagesRDict.xaml"/>
                <ResourceDictionary Source="./AddItemsRDict.xaml"/>
            </ResourceDictionary.MergedDictionaries>
        </ResourceDictionary>
    </Page.Resources>
    <Border Background="{StaticResource primary}" Name="AddBorder">
        <Grid x:Name="AddBaseGrid" Background="Transparent">
            <Grid.RowDefinitions>
                <RowDefinition Height="auto"/>
                <RowDefinition Height="auto"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="auto"/>
            </Grid.RowDefinitions>
            <Grid Grid.Row="0" >
                <Grid.RowDefinitions>
                    <RowDefinition/>
                    <RowDefinition/>
                    <RowDefinition/>
                </Grid.RowDefinitions>
                <CheckBox  Height="25" VerticalContentAlignment="Center" HorizontalAlignment="Center" Content="OnlyFiles" IsChecked="{Binding FilesOnly,Mode=TwoWay,UpdateSourceTrigger=PropertyChanged}"/>
                <Grid Grid.Row="1">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="auto"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>
                    <Button Background="{StaticResource primary}" Margin="3" Name="BackButton" Click="GoBack">
                        <Image Source="/images/up.png"/>
                    </Button>
                    <Label Grid.Column="1" Name="DirectoriesLabel" Grid.Row="1" Background="White"  VerticalContentAlignment="Center" HorizontalContentAlignment="Center"  >
                        <TextBlock FontWeight="ExtraBold" Foreground="Black" Text="Directories"/>
                    </Label>
                </Grid>
                <StackPanel Grid.Row="2"    >
                    <Label Content="Filter:" Background="{StaticResource primaryLight}" Foreground="{StaticResource textColor}" VerticalContentAlignment="Center" HorizontalContentAlignment="Center"/>
                    <Border   BorderBrush="{StaticResource primaryDark}" BorderThickness="2" Focusable="True">
                        <TextBox x:Name="FilterTBX" Text="{Binding Filter,Mode=TwoWay,UpdateSourceTrigger=PropertyChanged}" Background="White" Foreground="Black"/>
                    </Border>
                </StackPanel>
            </Grid>
            <DataGrid 
                PreviewKeyDown="AddDG_PreviewKeyDown"
                Style="{DynamicResource AddItemsDG}" x:Name="AddDG" Grid.Row="2" ItemsSource="{Binding Items}" 
                      MaxWidth="{Binding ElementName=AddBaseGrid,Path=ActualWidth}"
                      AutoGenerateColumns="False" CanUserAddRows="False" 
                      d:ItemsSource="{x:Static local:MockData.Items}">
                <DataGrid.RowStyle>
                    <Style  TargetType="{x:Type DataGridRow}">
                        <Setter Property="Background" Value="Transparent"/>
                        <Setter Property="IsSelected" Value="{Binding IsSelected,Mode=OneWay}"/>
                        <EventSetter Event="PreviewMouseLeftButtonDown" Handler="DataGridRow_MouseDown"/>
                        
                    </Style>
                </DataGrid.RowStyle>
                <DataGrid.Columns>
                    <DataGridTemplateColumn  Header="Name">
                        <DataGridTemplateColumn.CellTemplate>
                            <DataTemplate>
                                <StackPanel 
                                    Background="Transparent"
                                    DragDrop.PreviewDragEnter="HandleDragEnter" DragDrop.PreviewDragLeave="HandleDragLeave" Drop="StackPanel_Drop" Orientation="Horizontal">
                                    <StackPanel.ContextMenu>
                                        <ContextMenu>
                                            <MenuItem Header="Edit" Click="EditClickeds"/>
                                            <MenuItem Header="Delete" Click="DeleteClickeds" />
                                        </ContextMenu>
                                    </StackPanel.ContextMenu>
                                    <Button Click="openDirectory" CommandParameter="{Binding Path=.}" 
                                            PreviewMouseWheel="Button_PreviewMouseWheel"
                                            MouseEnter="Button_MouseEnter" MouseLeave="Button_MouseLeave">
                                        <Image Width="40" Height="40" Source="{Binding Image}"/>
                                    </Button>
                                    <TextBox Text="{Binding Name,Mode=TwoWay,UpdateSourceTrigger=PropertyChanged}" VerticalAlignment="Center" 
                                             PreviewKeyDown="NamePKeyDown"
                                             GotFocus="NameGotFocus" LostFocus="NameLostFocus"/>
                                </StackPanel>
                            </DataTemplate>
                        </DataGridTemplateColumn.CellTemplate>
                    </DataGridTemplateColumn>
                    <DataGridTemplateColumn Header="Description">
                        <DataGridTemplateColumn.CellTemplate>
                            <DataTemplate>
                                <TextBox HorizontalAlignment="Center" Text="{Binding Description,Mode=TwoWay,UpdateSourceTrigger=PropertyChanged}" VerticalAlignment="Center"/>

                            </DataTemplate>
                        </DataGridTemplateColumn.CellTemplate>
                    </DataGridTemplateColumn>
                    <DataGridTemplateColumn Header="Tags" Width="auto"
                                             
                                            >
                        <DataGridTemplateColumn.CellTemplate>
                            <DataTemplate>
                                <Grid Background="Transparent"
                                    MaxWidth="{Binding RelativeSource={RelativeSource AncestorType=DataGrid},Path=ActualWidth,Converter={StaticResource DoubleDividerConverter},ConverterParameter='4'}">
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="auto"/>
                                        <ColumnDefinition Width="*"/>
                                    </Grid.ColumnDefinitions>
                                    <Button Height="40" Width="40" Content="  +  " Margin="0 0 3 0" Background="{StaticResource addColor}" Click="AddTags" CommandParameter="{Binding RelativeSource={RelativeSource AncestorType=DataGridRow},Path=DataContext}"/>
                                    <ListView ItemsPanel="{StaticResource horizontalStackPanel}"
                                        Grid.Column="1" ItemsSource="{Binding Tags}" ScrollViewer.CanContentScroll="True" ScrollViewer.HorizontalScrollBarVisibility="Visible"
                                              MaxWidth="{Binding RelativeSource={RelativeSource AncestorType=DataGrid},Path=ActualWidth,Converter={StaticResource DoubleDividerConverter},ConverterParameter='4'}"
                                              d:ItemsSource="{x:Static local:MockData.Tags}" 
                                              Style="{StaticResource HorizontalSmallTransparantLV}"
                                              BorderBrush="Transparent" BorderThickness="0" ScrollViewer.VerticalScrollBarVisibility="Disabled"
                                              >
                                        <ListView.Resources>
                                            
                                        </ListView.Resources>
                                        <ListView.ItemTemplate>
                                            <DataTemplate>
                                                <Border Background="{Binding Color,Converter={StaticResource IntToBrushConverter}}">
                                                    <StackPanel>
                                                        <TextBlock  Text="{Binding Name}"/>
                                                        <Button Click="DeleteTag" CommandParameter="{Binding RelativeSource={RelativeSource AncestorType=DataGridRow},Path=DataContext}">X</Button>
                                                    </StackPanel>
                                                </Border>
                                            </DataTemplate>
                                        </ListView.ItemTemplate>
                                    </ListView>
                                </Grid>
                            </DataTemplate>
                        </DataGridTemplateColumn.CellTemplate>
                    </DataGridTemplateColumn>
                    <DataGridTemplateColumn Header="Original Path" Width="*">
                        <DataGridTemplateColumn.CellTemplate>
                            <DataTemplate>
                                <Button Content="{Binding OriginalPath}"  MouseDoubleClick="OpenOriginalPath" CommandParameter="{Binding OriginalPath}" Foreground="White"/>
                            </DataTemplate>
                        </DataGridTemplateColumn.CellTemplate>
                    </DataGridTemplateColumn>
                    <DataGridTemplateColumn Header="don't add to db">
                        <DataGridTemplateColumn.CellTemplate>
                            <DataTemplate>
                                <CheckBox IsChecked="{Binding IgnoreAdd}" Checked="DontAddToDbChecked" Unchecked="DontAddToDbUnchecked" DataContext="{Binding RelativeSource={RelativeSource AncestorType=DataGridRow},Path=DataContext}"/>
                            </DataTemplate>
                        </DataGridTemplateColumn.CellTemplate>
                    </DataGridTemplateColumn>
                    <DataGridTemplateColumn MinWidth="25" MaxWidth="25" Width="25">
                        <DataGridTemplateColumn.CellTemplate>
                            <DataTemplate>
                                <Button Height="25" VerticalAlignment="Center" Click="DeleteItem" CommandParameter="{Binding Path=.}"  Background="{StaticResource warnColor}" >
                                    <Image Source="/images/clear.png"/>
                                </Button>
                            </DataTemplate>
                        </DataGridTemplateColumn.CellTemplate>
                    </DataGridTemplateColumn>
                </DataGrid.Columns>
            </DataGrid>
            <Grid Grid.Row="6" Height="40" >
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>
                <Button Click="CancelClicked" Background="{StaticResource darkRed}" Margin="3">Cancel</Button>
                <Button Click="MoveClicked" Background="{StaticResource lightBlue900}" Grid.Column="1" Margin="3">Move</Button>
                <Button Click="CopyClicked" Background="{StaticResource addColor}" Grid.Column="2" Margin="3">Copy</Button>
            </Grid>
        </Grid>
    </Border>
</Page>
