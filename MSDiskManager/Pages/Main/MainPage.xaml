<Page x:Class="MSDiskManager.Pages.Main.MainPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      mc:Ignorable="d" 
      KeepAlive="True"
      d:DesignHeight="450" d:DesignWidth="800"
      Loaded="Page_Loaded"
      Title="MainPage">
    <DockPanel>
        <Frame NavigationUIVisibility="Hidden" DockPanel.Dock="Top"  x:Name="TopFrame" Height="200"/>
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition/>
                <RowDefinition Height="auto"/>
            </Grid.RowDefinitions>
            <UserControl Padding="20" x:Name="BottomControl" DockPanel.Dock="Bottom" />
            <Button Grid.Row="1" Margin="5" Height="30" Content="Add" Background="{StaticResource addColor}" Click="Button_Click"/>
        </Grid>
        
    </DockPanel>
</Page>
