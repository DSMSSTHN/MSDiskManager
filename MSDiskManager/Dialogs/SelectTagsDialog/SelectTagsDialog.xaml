<Window x:Class="MSDiskManager.Dialogs.SelectTagsWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:MSDiskManager.Dialogs"
        mc:Ignorable="d"
        Title="SelectTagsDialog" 
        WindowStartupLocation="CenterScreen"
        AllowsTransparency="True"
        Background="Transparent"
        WindowStyle="None"
        BorderBrush="Transparent"
        BorderThickness="0"
        ShowInTaskbar="False"
        Height="600" Width="800" Loaded="WindowLoaded">
    <Border CornerRadius="5"  BorderBrush="{StaticResource PrimaryLight}" BorderThickness="3">
        <Grid Loaded="Grid_Loaded" Background="{StaticResource Primary}">
            <Frame NavigationUIVisibility="Hidden" Padding="25" x:Name="SelectDialogMainFrame"/>
            <Button Width="25" Height="25" VerticalAlignment="Top" HorizontalAlignment="Right" Click="CloseClicked">
                <Image Source="/images/clear.png"/>
            </Button>
        </Grid>
    </Border>
</Window>
